---
interface Props {
  imgSrc: string;
  title: string;
  imgLeft?: boolean;
}
const { imgSrc, title, imgLeft } = Astro.props;
---

<div class="flex items-center gap-8">
  {
    imgLeft ? (
      <>
        <figure class="min-h-96 flex-1 object-cover">
          <img src={imgSrc} alt={title} class="h-full w-full object-cover" />
        </figure>
        <div class="flex flex-1 flex-col items-center gap-8">
          <h2 class="text-3xl font-semibold capitalize">{title}</h2>
          <p class="text-justify">
            <slot />
          </p>
        </div>
      </>
    ) : (
      <>
        <div class="flex flex-1 flex-col items-center gap-8">
          <h2 class="text-3xl font-semibold capitalize">{title}</h2>
          <p class="text-justify">
            <slot />
          </p>
        </div>
        <figure class="min-h-96 flex-1 object-cover">
          <img src={imgSrc} alt={title} class="h-full w-full object-cover" />
        </figure>
      </>
    )
  }
</div>
